#include <iostream>
using namespace std;

int main() {
    char name[4] = {'I', 'v', 'a', 'n'};
    int code = 3;

    cout << name[0] + code << endl;
    cout << name[1] + code << endl;
    cout << name[2] + code << endl;
    cout << name[3] + code << endl;
}